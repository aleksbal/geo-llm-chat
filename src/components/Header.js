import React from 'react';

function Header() {
  return (
    <header style={{ padding: '1rem', background: '#333', color: '#fff', textAlign: 'center' }}>
      <h1>LLM Dialog with Map Integration</h1>
    </header>
  );
}

export default Header;